apiVersion: v1
kind: Service
metadata:
  name: webchat-okteto-frontend
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
  selector:
    app: webchat-okteto-frontend