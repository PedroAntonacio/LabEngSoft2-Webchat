apiVersion: v1
kind: Service
metadata:
  name: webchat-okteto-backend
spec:  
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 5000
      name: http
  selector:
    app: webchat-okteto-backend